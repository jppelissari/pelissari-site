# Styles structure

- tokens.css → color, type, spacing, motion, radius, elevation
- base.css → reset, body, type primitives
- layouts.css → container, sections, grids, stacks, responsive layout
- components.css → header, nav, buttons, panels, chips, inputs, footer
- global.css → aggregator that imports all layers in order
